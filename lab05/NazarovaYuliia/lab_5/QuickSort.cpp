#include "Utils.h"

bool compareInt(const int& a, const int& b) { return a < b; }
bool compareDouble(const double& a, const double& b) { return a < b; }
bool compareString(const string& a, const string& b) { return a < b; }